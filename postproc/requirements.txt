numpy>=1.24
scipy>=1.10
matplotlib>=3.7
pandas>=2.0
pytest>=7.4
jupyter>=1.0
# Python SDK (issue #32): pybind11 provides the build-time headers + CMake config for bindings/.
# `pip install pybind11`, then build with scripts/build-python.sh (-DGNCSIM_BUILD_PYTHON=ON).
pybind11>=2.11
